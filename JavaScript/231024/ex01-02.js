// Number
let num = 100;
let floatNum = 3.14;

// String
let s1 = "string quote";
let s2 = "double quote";

// Boolean
let b1 = true;
let b2 = false;

// undefined
let emptyVal;

// null
let nullVal = null;

console.log(num, floatNum, s1, s2, b1, b2, emptyVal, nullVal);
// 100 3.14 'string quote' 'double quote' true false undefined null

console.log(
    typeof num,
    typeof floatNum,
    typeof s1,
    typeof s2,
    typeof b1,
    typeof b2,
    typeof emptyVal,
    typeof nullVal
);
// number number string string boolean boolean undefined object
